#include <iostream>
#include <cmath>
using namespace std;
/* // 158 int maxmin(int a[], int n, int &maxx, int &minn)
{
    maxx=a[0];
    minn=a[0];
    for(int i=0; i< n; i++)
    {
        if(a[i] > maxx)
            maxx=a[i];
        if(a[i] < minn)
            minn=a[i];
    }
}*/
int main()
{
    /* // 157 int x, n,lonnhat = 0;
    cin >> x;
    int a[6]={24,45,23,13,43,-12};
    for(int i =0; i<6; i++)
    {
        if((a[i] - x) > lonnhat)
        {
              lonnhat = (a[i]-x);
            n=a[i];
        }

    }
    cout << n;
    */



    /*int x;
    int maxx, minn;
    int arr[6]={24,45,23,13,43,-12};
    maxmin(arr,sizeof(arr)/sizeof(int),maxx, minn);
    if( minn >0)
        x=maxx;
    else
        if(abs(minn) > abs(maxx))
            x=minn;
        else
            x=maxx;
    cout << x;*/



    /*int n;
    cin >> n;
    float arr[n];
    for(int i=0; i<n; i++)
    {
        cin >> arr[i];
    }
    float cuoicung=0;
    for(int i=0; i<n; i++)
    {
        if(arr[i] >  -1  && arr[i] <0)
            {
                cuoicung=arr[i];
            }
    }
    cout << cuoicung;
    */

    /*int x, y, z = x;
    cin >> x >> y;
    int arr[6]={24,45,23,13,43,-12};
    for(int i=0; i<6; i++)
        if(arr[i]> x && arr[i] <y)
          {
            z= arr[i];
            break;
          }
    cout << z;
    */
    int n;
    cin >> n;
    float arr[n], z=-1;
    for(int i=0; i<n; i++)
    {
        cin >> arr[i];
    }
    for(int i=0; i<n; i++)
    {
        if(arr[i]== arr[i-1]*arr[i+1])
            z=i;

    }
    cout << z;
}
